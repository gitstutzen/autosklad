"""Делает libs/common импортируемым из тестов сервиса (репозиторий — монорепо,
но каждый сервис собирается и тестируется независимо, поэтому libs не установлен
как обычный пакет — просто добавляем корень репозитория в sys.path)."""
import sys
import os

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "..")))
