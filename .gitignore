__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
node_modules/
dist/
.DS_Store

# Секреты — НИКОГДА не коммитить.
# .env.example коммитится (это шаблон без значений), всё остальное — нет.
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Выгрузки боевых данных из пробников — содержат реальные заказы/клиентов
probe_output.json
probe_*.json

# Локальные базы проверок — тоже с боевыми данными
smoke.db
full_sync.db
*.db
